{
  "name": "murdeshwar-menu",
  "version": "1.0.0",
  "scripts": {
    "build": "if not exist dist mkdir dist && xcopy /E /I /Y * dist",
    "start": "serve dist"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
