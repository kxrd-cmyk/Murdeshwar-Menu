{
  "name": "murdeshwar-menu",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run clean && npm run copy",
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist && cp -r ./* dist/",
    "start": "serve dist"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}