{
  "name": "murdeshwar-menu",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run clean && npm run copy",
    "clean": "node -e 'require(\"fs-extra\").removeSync(\"dist\")' || true",
    "copy": "node -e 'require(\"fs-extra\").copySync(\".\", \"dist\", { filter: (src) => src !== \"./dist\" && src !== \"./dist/\" && src !== \"dist\" && src !== \"dist/\" && !src.startsWith(\"./dist/\") && !src.startsWith(\"dist/\") } )' || true",
    "start": "serve dist"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1",
    "serve": "^14.0.0"
  }
}