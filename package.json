{
  "name": "murdeshwar-menu",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run clean && npm run copy",
    "clean": "rmdir /s /q dist || true",
    "copy": "mkdir -p dist && xcopy /E /I /Y . dist",
    "start": "serve dist"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}